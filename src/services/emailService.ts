
export const send = async (
  toAddr: string[],
  ccAddr: string[],
  bccAddr: string[],
  subject: string,
  message: string,
): Promise<boolean> => {

  // TODO: Set this up once we get email working
  return false;
};
